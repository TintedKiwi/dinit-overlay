#!/bin/sh
dbus-uuidgen --ensure=/etc/machine-id
[ ! -L /var/lib/dbus/machine-id ] && ln -sf /etc/machine-id /var/lib/dbus/machine-id
[ ! -d /run/dbus ] && install -m755 -g 81 -o 81 -d /run/dbus

# start cgroup
/bin/bash /usr/lib/cgroup-utils/sv-cg.sh up dbus
