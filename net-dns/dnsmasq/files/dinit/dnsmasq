type            = process
command         = /usr/bin/dnsmasq -k --enable-dbus -u dnsmasq -g dnsmasq
before          = login.target
depends-on      = dbus
depends-on      = network.target
smooth-recovery = true
